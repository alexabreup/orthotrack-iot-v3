# OrthoTrack ESP32 Firmware - Environment Variables
# Copy this file to .env and update with your actual values

# ============================================
# WiFi Configuration
# ============================================
# SSID of the WiFi network the ESP32 will connect to
# Note: ESP32 only supports 2.4GHz networks (not 5GHz)
WIFI_SSID=orthotrack

# WiFi network password
WIFI_PASSWORD=L1vr3999$$$

# ============================================
# Backend API Configuration
# ============================================
# URL of the OrthoTrack backend API
# For local development: http://localhost:8080
# For production: https://api.orthotrack.com
# Make sure to use the correct IP/domain accessible from ESP32
API_ENDPOINT=http://localhost:8080

# ============================================
# Device Configuration
# ============================================
# Unique identifier for this ESP32 device
# Format: ESP32-WROOM32-XXX where XXX is a sequential number
# Each device must have a unique ID registered in the backend
DEVICE_ID=ESP32-WROOM32-001

# API Key for device authentication
# This key must be registered in the backend for this device
# The backend validates this key in the X-Device-API-Key header
API_KEY=orthotrack-device-key-2024

# ============================================
# Optional: Advanced Configuration
# ============================================
# These are configured in the code but can be overridden if needed

# Telemetry interval in milliseconds (default: 5000 = 5 seconds)
# TELEMETRY_INTERVAL=5000

# Heartbeat interval in milliseconds (default: 30000 = 30 seconds)
# HEARTBEAT_INTERVAL=30000

# Movement detection threshold in m/s² (default: 1.0)
# USAGE_THRESHOLD=1.0

# Battery ADC pin (default: 35)
# BATTERY_PIN=35

# ============================================
# Notes
# ============================================
# 1. After creating .env file, you need to modify platformio.ini to use these variables
# 2. Change build_flags to use ${sysenv.VARIABLE_NAME} syntax
# 3. The .env file is ignored by git for security
# 4. Never commit credentials to version control
# 5. For production, consider using ESP32 NVS or WiFi Manager instead of hardcoded credentials
